--- 3rdparty/llama.cpp/ggml/src/ggml.c
+++ 3rdparty/llama.cpp/ggml/src/ggml.c
@@ -1181,6 +1181,12 @@
     [GGML_TYPE_IQ4_NL] = "iq4_nl",
 };
 
+static void dequantize_row_i2_s_wrapper(const void * vx, float * y, int64_t n) {
+    const uint8_t * x = (const uint8_t *) vx;
+    const float i2_scale = ((const float *)(x + (n / 4)))[0];
+    dequantize_row_i2_s(x, y, n, i2_scale);
+}
+
 static const struct ggml_type_traits_t type_traits[GGML_TYPE_COUNT] = {
     [GGML_TYPE_I8] = {
         .type_name                = "i8",
@@ -1375,7 +1381,7 @@
         .is_quantized             = true,
-        .to_float                 = (ggml_to_float_t) dequantize_row_i2_s,
+        .to_float                 = dequantize_row_i2_s_wrapper,
         .from_float               = NULL,
         .from_float_ref           = NULL,
         .vec_dot                  = NULL,
@@ -13264,7 +13270,7 @@
                 }
             }
         } else {
-            if (gemm && (ne11 > 3)) {
+            if (false) {
                 if (src0->type == GGML_TYPE_I2_S) {
                     const float * scale = (float *)((uint8_t*)(src0->data) + (ne00 * ne01 / 4));
 
